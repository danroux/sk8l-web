```release-note:improvement
Set USER on Dockerfiles to make it possible to run the container as non root(securityContext). Split the process of replacing env vars to another step: Take it out of 'run_app.sh'. Update Docker base image.
```
